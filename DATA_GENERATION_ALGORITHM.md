# 전체 주택유형 부동산 합성 데이터 생성 알고리즘

## 결과와 실행법

`src/data_augmentation/generate.py`는 사용자가 지정한 개수만큼 연구용 합성
부동산 매물을 만든다. 실제 매물을 복사하지 않으며 주소·중개사·전화번호는 모두
합성 표식을 넣는다.

```powershell
cd D:\연구\FlexML\kb\jeonse_helper

# 기본 비율로 2만 건 생성
py -3 -m src.data_augmentation.generate --count 20000 --n-users 2000

# 머신러닝 가격모형을 끄고 경험분포 방식만 사용
py -3 -m src.data_augmentation.generate --count 20000 --price-model none

# 주택/거래 비율을 직접 지정
py -3 -m src.data_augmentation.generate --count 50000 --n-users 0 `
  --house-weights "아파트=0.4,오피스텔=0.2,단독주택=0.1,다가구주택=0.15,다세대주택=0.1,연립주택=0.05" `
  --transaction-weights "매매=0.3,전세=0.4,월세=0.3" `
  --seed 123
```

출력 파일은 다음과 같다.

- `data/generated/properties.csv`: 합성 매물
- `data/generated/users.csv`: 합성 사용자
- `data/generated/property_generation_quality.json`: 생성 품질·범위·제약 검증 결과

## 생성 범위

기본 설정은 다음 18개 조합을 모두 포함한다.

| 주택유형 | 매매 | 전세 | 월세 |
|---|---:|---:|---:|
| 아파트 | O | O | O |
| 오피스텔 | O | O | O |
| 단독주택 | O | O | O |
| 다가구주택 | O | O | O |
| 다세대주택 | O | O | O |
| 연립주택 | O | O | O |

기본 주택유형 비율은 아파트 32%, 오피스텔 24%, 단독 12%, 다가구 18%,
다세대 9%, 연립 5%다. 기본 거래 비율은 매매·전세·월세 각각 1/3이다.
비율은 CLI에서 바꿀 수 있으며, 요청 개수가 18건 이상이면 기본적으로 각 조합을
최소 1건 보장한다. 합계는 항상 사용자가 요청한 수와 정확히 일치한다.

### 전국 지역 커버리지

지역 기준은 HUG 시군구 사고현황의 17개 시도·252개 검색 단위다. 비자치구까지
포함하므로 행정안전부의 기초자치단체 수보다 세분화된 검색 기준이다. 각 거래유형
내에서 252개 지역을 순환 배정하므로 생성량이 756건 이상이면 모든 시군구에
매매·전세·월세가 각각 최소 1건 존재한다. 20,000건 기본 결과는 거래유형별
6,667·6,667·6,666건이며 각 시군구·거래유형 조합에 26~27건이 있다.

전국 RTMS 실거래 원본을 새로 수집한 것이 아니라 서울·경기 실거래의 주택유형·면적·
가격 결합분포를 전국 공간에 재배치한 **공간 프록시 합성 데이터**다. 시도별 가격은
KB 최신 아파트 평균 전세가격(만원/㎡)의 서울 대비 계수로 보정한다. 이 데이터는
전국 검색·Agent 실험용이며 지역별 실제 실거래 통계를 대체하지 않는다.

## 사용하는 다운로드 데이터

| 데이터 | 코드에서의 역할 |
|---|---|
| `rtms_apt_trade.csv` | 아파트 매매, 다세대·연립의 단위면적/입지 프록시 |
| `rtms_silv_trade.csv` | 아파트 분양권전매 가격 꼬리와 거래 하위유형 |
| `rtms_offi_trade.csv` | 오피스텔 매매 |
| `rtms_offi_rent.csv` | 서울 오피스텔 전세·월세 |
| `rtms_sh_trade.csv` | 단독·다가구 매매, 대지·연면적 |
| `rtms_sh_rent.csv` | 단독·다가구 전세·월세 |
| `gyeonggi_real_estate_portal_officetel_jeonse.csv` | 경기 오피스텔 전세·월세와 지역 다양화 |
| `kb_apt_average_sale_to_jeonse_price_ratio_monthly.csv` | 아파트 임대 합성 시 최신 지역별 전세가율 |
| `kb_apt_jeonse_average_price_by_region_monthly.csv` | 전국 시도별 서울 대비 가격계수 |
| HUG 지역 사고 자료 | 전세 합성 위험 라벨의 지역 효과 |
| 전국 CCTV 위치정보 | 시군구 대표 좌표 중앙값; 매물 주소와 시설 자체는 복제하지 않음 |

다음 다운로드 자료는 이 생성기에서 사용하지 않는다. 필요하지 않은 데이터를 억지로
넣으면 잘못된 상관관계와 데이터 누수가 생기기 때문이다.

- 소방서·치안센터·비상벨: 안전도구 전용
- 편의점 API 캐시: 생활편의 도구 전용
- CODEF 테스트자료·금융서비스 자료: 사용자 금융/상품검색 전용
- 전세사기 피해결정 통계: 실제 매물 가격·시설 생성과 직접 관계가 없어 미사용
- 주거 설문 대부분과 중복 KB 파일: 현재 직접 실거래/최신 전세가율보다 우선순위가
  낮고 중복가중을 피하기 위해 미사용
- 상업업무용 RTMS: 이번 6개 주거용 주택유형 범위 밖이므로 미사용

원본 파일은 `data/downloaded` 아래에서 읽기만 하고 수정하지 않는다.

## 직접지원과 교차자료 프록시

한 행마다 다음 메타데이터를 기록한다.

- `source_dataset`: 기준 원본 이름
- `source_record_hash`: 원본 행을 노출하지 않는 단방향 해시
- `source_transaction_type`: 기준 원본의 거래유형
- `generation_method`: 직접지원 조건부 부트스트랩 또는 교차자료 조건부 프록시
- `price_estimation_method`: 가격이 만들어진 방법
- `privacy_distance_score`: 기준 행과 합성 행의 면적·가격 상대 변화량

직접 원본이 있는 조합은 `직접지원 조건부 부트스트랩`이다. 현재 다운로드 폴더에
아파트 전월세 및 연립·다세대 실거래 파일은 없으므로 이 조합은 아래처럼 만든다.

- 아파트 전세·월세: 아파트 매매 실거래의 지역·면적·연식·가격에 최신 KB 지역별
  전세가율과 실제 임대 원본에서 관찰한 전월세 변환 구조를 결합한다.
- 다세대·연립: 아파트 단위매매의 지역·전용면적 구조에 각각 0.58, 0.66의 보수적인
  유형 가격계수를 적용하고 임대일 때 전세가율·전월세전환율을 결합한다.

따라서 후자의 행은 실제 다세대·연립 실거래에 직접 적합한 것으로 오해하면 안 된다.
향후 해당 RTMS 원본을 내려받으면 `_select_anchors`의 프록시 분기를 직접지원 풀로
교체할 수 있다.

## 알고리즘

### 1. 원본 정규화와 정제

서로 다른 API 컬럼을 내부 공통 앵커로 바꾼다. 공통 필드는 원본 종류, 거래유형,
주택유형, 시도·시군구·법정동, 거래월, 건축연도, 전용/연면적, 층, 거래금액,
보증금, 월세, 대지면적, 계약유형이다.

- 쉼표가 있는 금액은 만원 단위 숫자로 변환한다.
- `cdealType=O/1`인 취소공시는 매매 가격 기준에서 제외한다.
- 비정상 면적, 0 이하 매매가, 빈 지역 행은 제외한다.
- CSV 인코딩은 UTF-8-SIG, CP949, UTF-8 순으로 감지한다.

### 2. 비율 할당

주택유형 가중치와 거래유형 가중치의 곱으로 18개 조합의 기대 건수를 계산한다.
내림 후 남는 건수는 최대 나머지 방식으로 배분한다. 충분한 요청 수에서는 조합당
최소 1건을 먼저 보장한다.

그 다음 거래유형별 행을 전국 252개 시군구에 균등 층화한다. 한 거래유형의 행 수를
252로 나눈 몫만큼 전체 지역을 반복하고 나머지는 비복원 표본으로 배정한다. 따라서
특정 원본 지역의 빈도가 합성 DB 지역 커버리지를 독점하지 않는다.

### 3. 최신성 가중 조건부 표본

원본 거래가 최신월에서 `age`개월 전일 때 표본 가중치는 다음과 같다.

```text
w = exp(-log(2) × age / half_life_months)
```

기본 반감기는 18개월이다. 행 전체를 함께 뽑기 때문에 지역·면적·연식·가격·
전세/월세의 관측 결합이 독립 추출보다 덜 깨진다.

### 4. 연속형 합성

원본 값을 그대로 내보내지 않고 양수인 연속형에 작은 곱셈 잡음을 적용한다.

```text
면적 = 기준면적 × LogNormal(0, 0.025)        # 직접지원
면적 = 기준면적 × LogNormal(0, 0.060)        # 프록시
매매가 = 기준매매가 × 면적비^0.82 × 유형계수 × LogNormal(0, 0.045)
```

기본 `--price-model hedonic`에서는 위 경험가격을 그대로 끝값으로 쓰지 않는다.
아파트·오피스텔·단독/다가구 매매 실거래로 `log(단위면적가격)`을 예측하는
HistGradientBoosting 회귀를 학습한다.

```text
입력 = log(면적), 건축연령, 층, 거래월, 시군구, 원본 주택유형
목표 = log(매매가 / 면적)
```

각 주택유형의 최신 약 15% 거래월은 학습에서 제외해 시간 홀드아웃으로 평가한다.
직접 매매는 경험가격 70%와 ML 가격 30%를 로그 공간에서 혼합하고, 임대/프록시는
ML 가격을 시장가치 기준으로 쓴다.

예측값만 쓰면 평균으로 수축하므로 홀드아웃의 로그가격 잔차를 주택유형별로 복원
추출해 예측값에 더한다. 잔차의 0.5~99.5 백분위만 허용해 극단 오류는 막는다.
따라서 비선형 가격관계와 실제 가격 분산을 동시에 보존한다.

범주·주택유형은 기준 행의 조건부 문맥을 유지한다. 지역은 전국 층화 배정 후 KB
시도별 가격계수로 원본 시도 대비 가격을 조정한다. 실제 건물명,
상세주소, 중개사 정보는 사용하지 않고 전부 새 합성 값으로 만든다.

### 5. 전세·월세 금액

직접 임대 원본은 보증금과 월세를 함께 사용한다. 보증금을 흔든 차이는 연 3~9%의
전월세전환율로 월세에 반영한다.

```text
월세' = 원본월세 + (원본보증금 - 합성보증금) × 전환율 / 12
```

매매 기준에서 임대를 파생할 때는 다음 순서다.

1. 아파트는 최신 KB 지역별 매매가 대비 전세가율을 읽는다.
2. 다른 유형은 유형별 기준 전세가율에 분산을 준다.
3. 전세는 `시장가치 × 전세가율`, 월세는 그 환산보증금을 보증부월세 곡선 위에서
   나눈다.
4. 전세 월세액과 매매 보증금·월세액은 반드시 0으로 강제한다.

### 6. 건물·권리·중개 필드

주택유형별로 층수, 세대수, 주차, 엘리베이터, 방/욕실 수를 조건부 생성한다.
다가구 임대만 한 건물에 여러 임차인이 있는 권리구조로 취급한다. 아파트·오피스텔·
다세대·연립은 구분소유 호실을 기준으로 한다.

근저당, 채권최고액, 선순위 보증금, 신탁, 체납, 위반건축물은 실거래 API에 없는
값이므로 명확한 합성 가정이다. 이 값은 실제 등기·대장 조회를 대체하지 않는다.

### 7. 전세 위험 라벨

`fraud_label`은 전세에만 생성하고 매매·월세는 결측값이다. 경매회수 가능가 대비
부채, 후순위, 전세가율, HUG 지역사고율, 문서위험을 로짓에 넣어 베르누이 표본을
뽑는다. 실제 사기 확정 데이터가 아니라 모델 시험용 구조적 합성 정답이다.

## 211개 공인중개 매물 스키마

단일 기준은 `src/data_augmentation/property_schema.py`다. 다음 범주를 합친다.

1. 식별·합성 출처·생성방법·전국 지역배정·지역가격계수·좌표출처
2. 소재지·토지대장·토지이용·도로와 지번
3. 매매가·보증금·월세·관리비·계약기간·중개보수
4. 건축물대장·동/호·면적·층·방향·세대수
5. 주차·승강기·냉난방·가전·상태·수리·안전설비
6. 소유·등기·근저당·선순위·체납·임대보증·전세 위험
7. 교통·생활편의·소음·침수 등 입지
8. 중개사·표시광고·사진/영상·방문 방식
9. 확인·설명 근거서류와 완료 여부

이 스키마는 공인중개 실무 및 주거용 확인·설명/표시광고 항목을 폭넓게 담은
프로젝트용 canonical schema다. 민간 포털별 비공개 제휴필드까지 “모두” 포함한다고
주장하지 않는다.

## 검증

생성 직후 다음을 자동 검사한다.

- 요청 행 수와 실제 행 수 일치
- 211개 canonical/감사 컬럼과 기존 모델 필수 컬럼 존재
- 주택·거래유형 허용값
- 매매의 보증금/월세 0, 전세의 월세 0
- 양수 면적·시장가치, 순위≤세대수
- 합성 표식, 고유 ID, 합성 주소와 중개사번호
- 주택유형×거래유형 커버리지
- 전국 17개 시도·252개 시군구 및 모든 시군구×매매/전세/월세 조합 존재

상세 결과는 `property_generation_quality.json`에 저장된다.
