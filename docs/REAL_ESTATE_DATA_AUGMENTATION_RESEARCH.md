# 부동산 합성 데이터 생성 참고 연구와 적용 내역

## 선택한 접근

현재 원본은 수십만 건의 혼합형 표 데이터이고 주택유형별로 사용할 수 있는 컬럼이
다르다. 최종 구현은 **조건부 경험 부트스트랩 + 헤도닉 gradient boosting +
홀드아웃 잔차 부트스트랩 + 도메인 제약**의 하이브리드다. 가격의 비선형 관계에는
머신러닝을 쓰되, 모든 속성을 불투명한 생성모형에 맡기지는 않는다.

## 직접 반영한 연구

### 1. Reiter (2005), Using CART to Generate Partially Synthetic Public Use Microdata

- 논문: <https://www.scb.se/contentassets/ca21efb41fee47d293bbee5bf7be7fb3/using-cart-to-generate-partially-synthetic-public-use-microdata.pdf>
- 핵심: 혼합형 마이크로데이터는 변수를 독립적으로 흔들기보다 이전 변수에 조건부인
  분포에서 순차 생성해 결합관계를 보존해야 한다.
- 적용: 코드가 CART 자체를 학습하지는 않지만 `주택유형×거래유형`을 먼저 정하고,
  지역·면적·연식·가격·계약조건이 함께 있는 원본 행 전체를 조건부 표본으로 뽑는다.
  연속값만 작은 잡음과 제약으로 변형한다.

### 2. Reiter (2005), Releasing Multiply Imputed, Synthetic Public Use Microdata

- 논문: <https://doi.org/10.1111/j.1467-985X.2004.00343.x>
- 핵심: 합성 자료는 통계적 유용성과 공개 위험을 함께 평가해야 하며, 생성모형과
  합성 범위를 명시해야 한다.
- 적용: 실제 주소·중개사·전화번호·원본 식별자를 출력하지 않는다. 원본 연결은
  단방향 `source_record_hash`만 남기고, `generation_method`,
  `price_estimation_method`, `privacy_distance_score`를 함께 저장한다.

### 3. Xu et al. (2019), Modeling Tabular Data using Conditional GAN (CTGAN)

- 논문: <https://arxiv.org/abs/1907.00503>
- 핵심: 표 데이터는 다봉 연속분포와 불균형 범주 때문에 단일 정규분포·독립 표본으로
  만들기 어렵고, 범주 조건부 학습과 균형 표본이 중요하다.
- 적용: GAN 자체는 사용하지 않았지만 최대 나머지 할당으로 희소한 18개 조합을
  보장하고, 가격·면적은 단일 전역 분포가 아니라 주택유형·거래유형별 실제 행과
  로그 곱셈 잡음으로 생성한다.
- 미적용 이유: 현재 목표는 생성근거 추적과 즉시 재현 가능한 CPU 파이프라인이다.
  CTGAN을 추가하려면 학습/홀드아웃 분리, 하이퍼파라미터 반복, 프라이버시 공격
  평가가 추가로 필요하다.

### 4. Yang et al. (2024), Structured Evaluation of Synthetic Tabular Data

- 논문: <https://arxiv.org/abs/2403.10424>
- 핵심: 합성 자료 평가는 단일 주변분포만 보지 말고 구조적 분해를 통해 결합분포,
  다운스트림 유용성, 모델 기반 지표를 함께 봐야 한다. 작은 표에서는 구조를 명시한
  생성기가 딥러닝 생성기보다 나을 수 있음을 보고한다.
- 적용: 품질 보고서에 주택유형×거래유형 커버리지, 지역/원본/생성방법별 건수,
  주요 연속값 분위수, 도메인 제약, 합성 주소·중개사 표식 검사를 분리해 기록한다.

### 5. Bajari and Kahn (2003/2005), Estimating Housing Demand with a Non-parametric Hedonic Home Price Function

- 논문: <https://www.nber.org/papers/w9891>
- 핵심: 주택은 면적·입지·품질 등 특성 묶음이고 가격은 이 특성들의 함수로 다루어야
  한다.
- 적용: 매매가를 독립 재추출하지 않고 기준 매매가에 면적비의 탄력도, 주택유형
  계수, 지역 전세가율, 제한된 로그 잡음을 적용한다. 다만 현재 계수는 별도 회귀로
  추정한 구조모수라고 주장하지 않고 보수적인 생성 규칙으로 문서화한다.

### 6. Park and Bae (2015), Using Machine Learning Algorithms for Housing Price Prediction

- 논문: <https://doi.org/10.1016/j.eswa.2014.11.040>
- 핵심: 실제 MLS 주택 가격은 주택의 구조·입지 특성을 함께 사용할 때 예측력이
  생기며, 교차검증으로 일반화를 평가해야 한다.
- 적용: 합성 가격과 시설값이 면적·연식·주택유형·층·세대수와 연결되도록 만들었다.
  가격모형은 원본 실거래의 최신기간 홀드아웃으로 검증한다.

### 7. Friedman (2001), Greedy Function Approximation: A Gradient Boosting Machine

- 논문: <https://doi.org/10.1214/aos/1013203451>
- 핵심: 회귀나무를 함수공간의 단계적 경사하강으로 결합하면 비선형 회귀함수를
  유연하게 근사할 수 있다.
- 적용: `HistGradientBoostingRegressor`로 지역·면적·연식·층·거래월과
  단위면적가격의 비선형 관계를 학습한다. 최신 약 15% 거래를 주택유형별로 홀드아웃해
  시간 외삽 성능을 기록한다.

### 8. Freedman (1981), Bootstrapping Regression Models

- 논문: <https://doi.org/10.1214/aos/1176345638>
- 핵심: 회귀모형의 잔차를 재표본화하는 bootstrap의 통계적 성질을 다룬다.
- 적용: 최신기간 홀드아웃의 `실제 log(㎡당가격)-예측 log(㎡당가격)` 잔차를
  주택유형별 경험분포로 저장하고, 합성 예측가격에 다시 더한다. 이 때문에 ML의
  평균회귀로 사라질 가격 분산과 꼬리 일부가 복원된다.

### 9. Umesh et al. (2024), Preserving Logical and Functional Dependencies in Synthetic Tabular Data

- 논문: <https://arxiv.org/abs/2409.17684>
- 핵심: 여러 최신 표 합성모형도 속성 간 함수적·논리적 의존성을 완전히 보존하지
  못할 수 있으므로 작업별 구조 보존이 필요하다.
- 적용: 매매 보증금=0, 전세 월세=0, 순위≤세대수, 면적 순서, 구분소유/다가구
  권리구조 같은 규칙은 ML이 아니라 명시적 후처리 제약으로 강제한다.

## 프라이버시·유용성 평가에서 추가로 참고한 연구

### Little, Allmendinger and Elliot (2025), Synthetic Census Microdata Generation

- 논문: <https://doi.org/10.1177/0282423X241266523>
- 시사점: 생성기 간 비교에는 유용성뿐 아니라 재식별/공개 위험의 교환관계를 같이
  봐야 한다.
- 현재 적용 수준: 원문 주소·건물명·개인/중개사 식별자를 제거하고 합성 표식을
  강제한다. 다만 RTMS는 공개 거래자료이므로 차등프라이버시 보장을 주장하지 않는다.

## 적용하지 않은 방법

| 방법 | 이번 구현에서 제외한 이유 | 향후 적용 조건 |
|---|---|---|
| CTGAN/TVAE | 학습비용보다 설명 가능성과 조합 보장이 우선 | 주택유형별 원본 추가, 홀드아웃 평가, 반복실험 |
| Gaussian Copula | 범주·가격의 조건부 다봉성과 공간 구조를 단일 copula가 과도하게 단순화할 수 있음 | 유형별 copula와 꼬리 적합도 비교 |
| SMOTE | 분류 경계 보간법이라 매물 전체 레코드 생성에 부적합 | 사기 라벨 학습셋의 소수 클래스 보강에만 제한 사용 |
| LLM 행 생성 | 수치분포·제약 재현과 반복 가능성이 낮음 | 설명문 같은 비정형 텍스트 필드에만 제한 사용 |
| 차등프라이버시 GAN | 공개 RTMS 기반 현재 목적에 비해 구현·평가 비용이 큼 | 비공개 중개사/고객 원본을 학습할 때 필수 검토 |

## 해석 제한

1. 아파트 전월세, 다세대·연립은 현재 직접 RTMS 원본이 없어서 교차자료 프록시다.
2. 등기·체납·선순위·위반건축물·시설·중개사 필드는 실측이 아니라 조건부 합성이다.
3. 합성 위험 라벨은 사기 확정 사실이 아니다.
4. 합성 좌표는 지역 중심 주변의 가상 좌표이며 실제 건물 위치가 아니다.
5. 생성 데이터는 모델 개발·GUI 시험용이며 실제 계약, 감정평가, 권리분석에 사용할
   수 없다.

## 2026-07-15 구현 검증 결과

최종 2만 건 생성 시 최신기간 홀드아웃 성능은 다음과 같다.

- `log(㎡당가격)` R²: 0.7552
- ㎡당가격 중앙 절대백분율오차(MdAPE): 17.68%
- 스키마/금액/논리 제약 오류: 0건

동일 시드 3,000건으로 ML을 끈 방식과 비교했다.

| 지표 | 경험분포만 사용 | GBDT+잔차 bootstrap |
|---|---:|---:|
| 전체 매매 log가격 표준편차 | 0.9486 | 0.9178 |
| 아파트 면적-log가격 상관 | 0.7211 | 0.7470 |
| 오피스텔 면적-log가격 상관 | 0.7702 | 0.8006 |
| 다가구 면적-log가격 상관 | 0.6051 | 0.6688 |

ML 방식은 주요 구조관계를 강화하면서 비ML 가격분산의 약 96.8%를 유지했다.
따라서 현재는 ML을 기본값으로 선택하되 `--price-model none`으로 언제든 재현 가능한
비ML 기준선과 비교할 수 있게 했다.

## 다음 연구 단계

1. 아파트 전월세 및 연립·다세대 RTMS 원본을 추가해 프록시를 직접지원으로 교체한다.
2. 거래월 기준 학습/검증 분할 후 원본과 합성의 1D KS, 범주 TVD, 2D 조건부 거리,
   TSTR(Train Synthetic, Test Real)을 측정한다.
3. 조건부 부트스트랩, CART, Gaussian Copula, CTGAN을 동일한 홀드아웃으로 비교한다.
4. 원본 최근접거리, 속성추론, 멤버십추론으로 공개 위험을 별도 측정한다.
