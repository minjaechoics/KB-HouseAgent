# 집 선택 별 자산 시뮬레이션 및 AI 웹 검색 설계

## 계산 흐름

1. 사용자가 지도에서 집을 선택한다.
2. `POST /api/properties/report`가 자산 입력과 선택 금융상품을 검증한다.
3. `src/report/lifestyle.py`가 목적지를 지오코딩하고 목적지당 경로를 한 번 조회한다.
   한 요청의 목적지는 최대 5개다.
4. 대중교통은 TMAP 운임을 우선 사용한다. 운임이 없으면 거리 기반 추정값임을
   응답에 표시한다. 자가용은 왕복거리, 연비, 유류단가와 월 보험·정비·주차비를
   합산한다.
5. 식비는 `하루 식비 × 30.4`, 구독·통신·인터넷·보험·여가·기타는 월 금액으로
   합산한다. `항목별 예산 사용`을 켜면 첫 화면의 통합 생활비를 대체하므로 중복
   계산하지 않는다.
6. `src/report/budget.py`가 선택 금융상품의 금리·한도, 계약자금, 소득·물가·금융
   자산 수익률과 월 지출을 종료 나이까지 연 단위로 누적한다.
7. 현금성 금융자산과 집값 전망 지수는 별도 그래프로 표시한다. 전·월세의 집값
   지수는 참고 전망일 뿐 사용자 순자산에 합산하지 않는다. 매매만 주택가치와
   대출잔액을 순자산에 반영한다.

이 결과는 세금, 실제 대출 심사, 상환방식 변경, 실직, 조기상환 등 모든 사건을
예측하는 재무자문이 아니라 입력 가정에 따른 조건부 시뮬레이션이다.

## 구독료

주요 서비스 기본값은 검색 가능한 목록으로 제공하지만 가입 경로, 요금제,
프로모션에 따라 바뀔 수 있다. 모든 금액은 사용자가 수정할 수 있고 화면에도
참고값임을 표시한다.

## AI 인터넷 검색 도구

조건 추가 대화는 먼저 사용자의 모호함을 질문하고 버튼 승인을 받는다. 승인 뒤
지도 지오코딩이 실패한 장소만 `APILLM.web_search()`로 한 번 확인한다.

- OpenAI Responses API의 `web_search` 도구를 사용한다.
- 공식 기관·공식 장소 페이지를 우선하도록 지시한다.
- 검색 결과의 정식명/도로명주소를 다시 NAVER 지오코딩으로 검증한다.
- 지도 검증을 통과한 경우에만 Text-to-SQL과 UI 조건으로 만든다.
- 웹 출처 URL은 응답과 RAG DEBUG trace에 남기고 채팅에서 클릭할 수 있게 표시한다.
- 검색이나 재검증에 실패하면 좌표를 만들지 않고 사용자에게 정확한 주소를 묻는다.

관련 코드:

- `src/agent/llm.py`: Responses 웹 검색 호출, 재시도, 출처 수집
- `src/server/app.py`: 지오코딩 실패 시 검색 1회 및 지도 재검증
- `src/server/gui.html`: 웹 출처 링크와 RAG trace 표시
